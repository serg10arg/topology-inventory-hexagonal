package com.example.topologyinventory.framework.adapters.output.h2.data;

import jakarta.persistence.Embeddable;

/** Espejo de persistencia del value object {@code SwitchType} (LAYER2 / LAYER3). */
@Embeddable
public enum SwitchTypeData {
    LAYER2, LAYER3
}
