<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>topology-inventory</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>domain</module>
        <module>application</module>
        <module>framework</module>
        <module>bootstrap</module>
    </modules>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <lombok.version>1.18.34</lombok.version>
        <junit.version>5.11.3</junit.version>
        <compiler-plugin.version>3.13.0</compiler-plugin.version>
        <surefire-plugin.version>3.5.2</surefire-plugin.version>
        <jakarta.persistence.version>3.1.0</jakarta.persistence.version>
        <eclipselink.version>4.0.4</eclipselink.version>
        <h2.version>2.2.224</h2.version>

        <!--
         ~ Plataforma Quarkus. Se fija la LTS vigente, recomendada para produccion.
         ~ El BOM 'quarkus-bom' (importado abajo) alinea por construccion las
         ~ versiones de todas las extensiones Quarkus, igual que 'junit-bom' alinea
         ~ jupiter con platform. Sustituye al antiguo 'quarkus-universe-bom' del
         ~ libro (discontinuado tras Quarkus 2.x).
         -->
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.version>3.33.2.1</quarkus.platform.version>

        <!--
         ~ Plugin Jandex: genera el indice de clases (META-INF/jandex.idx) de cada
         ~ modulo. Necesario porque los @Entity (y, mas adelante, los CDI beans)
         ~ viven en jars separados (framework, application) que Quarkus NO indexa
         ~ automaticamente: solo indexa el modulo que arranca el motor (bootstrap).
         ~ Coordenada modernizada respecto al libro: 'org.jboss.jandex' paso a
         ~ 'io.smallrye' en el ecosistema Quarkus 3.x.
         -->
        <jandex-plugin.version>3.5.3</jandex-plugin.version>
    </properties>

    <!--
     ~ Gestión centralizada de versiones. El BOM de JUnit alinea por construcción
     ~ junit-jupiter (5.x) con junit-platform (1.x), que llevan numeración distinta:
     ~ importarlo evita tener que recordar qué platform pareja con qué jupiter. El
     ~ BOM de Quarkus cumple el mismo papel para las extensiones Quarkus.
     -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!--
             ~ BOM de la plataforma Quarkus. No trae ninguna extension al classpath
             ~ por si mismo: solo pone a disposicion versiones coherentes. Las
             ~ extensiones (p. ej. quarkus-arc) se declaran SIN <version> alli donde
             ~ se necesiten; el BOM la resuelve.
             -->
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>jakarta.persistence</groupId>
                <artifactId>jakarta.persistence-api</artifactId>
                <version>${jakarta.persistence.version}</version>
            </dependency>
            <dependency>
                <groupId>org.eclipse.persistence</groupId>
                <artifactId>org.eclipse.persistence.jpa</artifactId>
                <version>${eclipselink.version}</version>
            </dependency>
            <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>${h2.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
            </plugin>
            <!--
             ~ Indexado Jandex para TODO el reactor. En modulos con clases (domain,
             ~ application, framework) el goal 'jandex' se engancha a 'process-classes'
             ~ y deja META-INF/jandex.idx dentro del jar; en el modulo padre
             ~ (packaging pom) y en bootstrap es inofensivo. Sin este indice, la
             ~ augmentation de Quarkus no descubriria las entidades de otros modulos.
             -->
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <version>${jandex-plugin.version}</version>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals>
                            <goal>jandex</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>